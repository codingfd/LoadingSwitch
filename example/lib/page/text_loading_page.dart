
import 'package:flutter/material.dart';

class TextLoadingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("加载中",style: TextStyle(fontSize: 24),),
    );
  }
}
